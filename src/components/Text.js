import styled from 'styled-components';
import { color, margin, padding } from 'styled-system';
import typography from '@styled-system/typography';

export default styled.p`
  ${color}
  ${margin}
  ${padding}
  ${typography}
`;

export const InsideText = styled.span`
  ${color}
  ${margin}
  ${padding}
  ${typography}
`;
